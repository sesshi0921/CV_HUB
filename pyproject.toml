[project]
name = "cvhub-python"
version = "0.1.0"
description = "Python worker environment for CV_HUB plugins"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.12.0",
    "torchvision>=0.27.0",
    "Pillow>=12.0.0",
    "numpy>=2.0.0",
]
